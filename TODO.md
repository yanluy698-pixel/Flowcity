# FlowCity TODO

## 当前阶段：阶段五 - Validator 与重排

### 已完成

- [x] 定义 `schema.json`
- [x] 定义 `prompt.md`
- [x] 定义 `examples.json`
- [x] 编写 `extractor.py`
- [x] 接入 DeepSeek OpenAI 兼容 API 配置
- [x] 启用 DeepSeek JSON Output
- [x] 添加 `.env.example`
- [x] 添加 `.gitignore`
- [x] 初始化项目说明文档

### 阶段二优化

- [x] 微调 `prompt.md`，避免模型把“喜欢的女生”直接判断为稳定情侣关系。
- [x] 微调 `prompt.md`，减少模型自动补充用户未明确表达的偏好，例如“少排队”“citywalk”。
- [x] 补充更多开放式输入测试样例。
- [x] 将 `basic_validate` 升级为完整 Schema 子集校验。
- [x] 增加测试脚本，批量跑 `examples.json`。
- [ ] 保存每次模型输出样例，形成评测记录。

## 阶段三：数据与 Mock API

- [x] 设计活动 POI 数据结构。
- [x] 设计餐厅 POI 数据结构。
- [x] 设计路线/通勤时间 Mock 数据。
- [x] 设计排队状态 Mock 数据。
- [x] 设计预约时段 Mock 数据。
- [x] 设计团购库存 Mock 数据。
- [x] 编写函数版 Mock API。
- [x] 实现基础筛选逻辑：硬约束过滤。
- [x] 实现基础排序逻辑：软偏好打分。
- [x] 增加阶段二 + 阶段三串联脚本 `run_flow.py`。
- [x] 增加可选 FastAPI 包装层 `api.py`。
- [x] 增加定向活动硬约束过滤，例如滑雪无供给时明确失败，不推荐展览替代。
- [x] 增加 `supplyStatus`，返回阶段三机器事实层的供给状态和失败约束。
- [x] 区分“不想花钱”和严格预算 0，免费/低消费作为排序信号处理。
- [x] 增加咸阳到西安市区的入城路线 Mock 数据，并把路线成本挂载到候选项。
- [x] 增加 Mock API 行为断言，校验首位候选是否符合关键约束。

### 阶段三待优化

- [ ] 编写更完整的 Mock API 接口说明文档。
- [ ] 补充更多西安商圈和 POI 动态状态。
- [ ] 增加前端卡片选项对应的偏好字段测试样例。
- [ ] 安装并验证 FastAPI/uvicorn HTTP 版本。

## 阶段四：AI 规划能力

- [x] 设计 Planner 输入格式。
- [x] 设计时间轴方案输出格式。
- [x] 基于阶段二结构化需求和阶段三 Mock 数据生成方案。
- [x] 输出推荐理由、预算估算和风险提示。
- [x] 增加规则约束下的 LLM Planner Prompt。
- [x] 增加离线确定性草案，保证无 Key/无网络时测试可跑。
- [x] 校验 Planner 输出字段和候选引用合法性。
- [x] 接入 `run_flow.py`，形成 `input -> structuredDemand -> mockSupply -> timelinePlan` 链路。
- [x] 增加 Planner JSON 解析失败重试，降低真实 LLM 输出截断造成的失败率。
- [x] 强化路线、预算、跨城一致性校验，避免 Planner 编造路线或漏算入城成本。
- [x] 修复低成本语义回归：想花钱少是偏好低成本，不等于严格预算 0。
- [x] 拆分预算语义三档：低成本偏好、免费优先、免费硬约束。
- [x] 让 `预算0元/只能免费` 在 Mock API 层真正硬过滤收费候选。
- [x] 用 4 条真实 LLM 输入回归验证预算语义链路。

### 阶段四待优化

- [ ] 固化 3 条稳定演示输入，并保存对应 `timelinePlan` 黄金样例。
- [ ] 把 20 条真实 LLM 测试报告和 4 条预算语义测试沉淀为自动化回归用例。
- [ ] 增加 Planner 输出的人类可读渲染层，减少 Demo 时直接阅读 JSON。
- [ ] 为阶段五 Validator 准备更细的时间、营业时段、票座、排队 Mock 字段。

## 阶段五：Validator 与重排

- [x] 实现时间窗口校验。
- [x] 实现营业时间校验。
- [x] 实现预算校验。
- [x] 实现适龄/人群适配校验。
- [x] 实现座位/余票/排队风险校验。
- [x] 实现失败原因结构化输出。
- [x] 实现 Replanner 局部替换策略。
- [x] 接入 `run_flow.py`，输出 `validationResult` 和 `replanResult`。
- [x] 增加阶段五回归测试，覆盖低成本偏好、免费优先和免费硬约束。

## 阶段六：执行链路

- [ ] Mock 订票。
- [ ] Mock 餐厅预约。
- [ ] Mock 线上取号。
- [ ] Mock 团购下单。
- [ ] 生成订单号/预约号。
- [ ] 生成分享文案。

## 阶段七：Demo 展示

- [ ] 设计 Web UI 或命令行 Demo。
- [ ] 展示用户输入。
- [ ] 展示结构化需求。
- [ ] 展示工具调用日志。
- [ ] 展示时间轴方案。
- [ ] 展示 Validator 校验结果。
- [ ] 展示确认执行结果。

## 大推进路径：20 天冲刺主线

> 原有 TODO 保留为模块细节；本节作为接下来 20 天的主推进路径。

### 第 1 步：今天先打穿闭环

- [x] 定义并固定主数据流：`structuredDemand -> mockSupply -> timelinePlan`。
- [x] 新增 `planner.py`，把候选活动、餐厅、路线组合成可执行时间轴方案。
- [ ] 新增 `validator.py`，校验时间窗口、预算、余票、座位、排队、人群适配和路线折腾程度。
- [ ] 新增 `executor.py`，Mock 锁票、预约、团购下单、线上取号、分享文案生成。
- [ ] 升级 `run_flow.py`，支持一条命令输出完整链路：需求拆解、供给查询、时间轴方案、校验结果、执行结果。

### 第 2 步：3 天内做出可演示产品

- [ ] 做一个最小 Web UI 或强展示型 CLI Demo。
- [ ] Demo 展示完整流程：用户输入 -> AI 理解 -> 工具调用 -> 方案时间轴 -> 风险校验 -> 确认执行。
- [ ] 页面或命令行输出要突出“过滤原因”“推荐理由”“可执行动作”，不能只展示 JSON。
- [ ] 准备 3 条固定演示输入，确保每条都能稳定跑完闭环。

### 第 3 步：打磨 3 个强场景

- [ ] 亲子 + 减脂老婆 + 预算 + 少走路：突出人群适配、饮食约束、排队/座位。
- [ ] 朋友 citywalk + 小吃 + 不想走太多：突出矛盾需求处理和路线折中。
- [ ] 咸阳到西安 + 人均 100 + 地铁：突出跨城入城、预算消耗、时间可行性。
- [ ] 每个场景都要有成功方案、过滤解释、风险提示和可执行确认结果。

### 第 4 步：做出差异化能力

- [ ] 硬约束失败时明确说明失败原因，不强行推荐不相关内容。
- [ ] 实现 Replanner：餐厅无座、活动无票、预算超限时局部替换方案。
- [ ] 支持多方案对比：省钱版、轻松版、体验版。
- [ ] 执行前二次校验：确认前重新检查余票、座位、排队和预算。
- [ ] 生成可转发分享文案，让方案看起来像真正完成了一次出行组织。

### 第 5 步：补商业表达和比赛材料

- [ ] 用 2 页以内设计文档讲清楚 Planning 策略、工具调用链路、异常处理机制。
- [ ] 梳理美团业务价值：提升搜索到交易转化、组合活动与餐饮供给、降低用户决策成本、给商家带来增量分发。
- [ ] 准备一键运行说明和稳定演示脚本。
- [ ] 最后 2 天停止大功能新增，专注稳定性、录屏、README 精简和答辩话术。
